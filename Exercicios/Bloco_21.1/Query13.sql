USE hr;
-- atividade 1
SELECT MAX(MAX_SALARY) AS MAIOR_SALARIO FROM jobs;
-- atividade 2
SELECT MAX(MAX_SALARY) DIV MIN(MIN_SALARY) AS DIFERENTE FROM jobs;
-- atividade 3
SELECT  JOB_ID, AVG(SALARY) AS 'MEDIA_SALARIAL'
FROM hr.employees
GROUP BY JOB_ID
ORDER BY `MEDIA_SALARIAL` DESC;